CLAUDE.md
.venv/
__pycache__/

# Node / Vite (frontend)
node_modules/
dist/
*.local

# Logs
*-debug.log*
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Env / secrets (LLM API keys sau này)
.env
.env.*
!.env.example

# SQLite conversation history (local dev runs; prod stores it on a Docker volume)
*.db
*.db-shm
*.db-wal

# Benchmark result cache (re-fetchable; avoids re-spending Gemini quota)
tests/quality_bench/data/.cache/

# OS / editor
.DS_Store
Thumbs.db